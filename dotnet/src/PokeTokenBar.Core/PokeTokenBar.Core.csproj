<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!-- So internal helpers can be tested directly rather than promoted to public API for the
         sake of a test. The URL-id parser in particular is security-relevant and deserves its
         own cases. -->
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
      <_Parameter1>PokeTokenBar.Core.Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>

</Project>
