# OMC / agent local state
.omc/

# .NET (C# port)
bin/
obj/
*.user

# VS Code extension (C# port host)
node_modules/
*.vsix
extension/out/
extension/dist/
# Staged sidecar binaries — built per platform, never committed
extension/server/

# macOS
.DS_Store

# Signing material — never commit private keys / certs
*.p12
*.pem
*.key
*.cer
*.certSigningRequest

# Agent worktrees / local state
.claude/
.worktrees/

# Local-only review-gate opt-out (not published)
.no-review-gate
