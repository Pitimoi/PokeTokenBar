.vscode/**
src/**
scripts/**
node_modules/**
tsconfig.json
**/*.ts
**/*.map

# server/ is intentionally NOT ignored: the platform-specific sidecar binary ships in the VSIX.
# Downloading it on first run was rejected — it would reintroduce the unverified-artifact
# problem the Swift release pipeline has today.
