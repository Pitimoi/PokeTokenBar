{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build: sidecar + extension",
      "type": "shell",
      "command": "dotnet build -c Release ../dotnet/PokeTokenBar.slnx && npm install --no-audit --no-fund && npm run dev",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$tsc", "$msCompile"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
